# VC-50 아인-카테고리 경계 이해 — 서비스 흐름도 v5

상태: REWORKED_SOURCE_ALIGNED

## 1. 원문 대조

- 상황: 같은 제품이 `다시 시작`, `Analog`, `선물` 등 여러 위치에 나타나 중복 제품인지 다른 구성인지 헷갈린다.
- 목표: 카테고리 기준과 다중 소속 이유를 이해하고 같은 제품을 중복 후보로 세지 않는다.
- 핵심 과업·행동 흐름: 카테고리 정의 → 제품 소속 → 다른 분류 경로 → 동일 Product 확인.
- 요구: 분류 기준 설명, 동일 Product 유지, 주·보조 소속 구분, 중복 수량 제거.
- 성공 조건: 같은 제품과 다른 변형을 구분하고 현재 카테고리 포함 이유를 설명한다.
- 실패 조건: 이름·이미지만 바꿔 중복 제품을 새 후보로 계산한다.
- 연결 제품: PROD-030·PROD-060·PROD-100

## 2. 클라이언트 전용 흐름

    flowchart TD
    A[관련 진입] --> B[상황·목표 확인]
    B --> C[원문 과업·행동 수행]
    C --> D[요구 기반 콘텐츠·제품·기능 확인]
    D --> E{근거·상태 이해}
    E -- 아니요 --> F[설명·HOLD·대체 경로]
    F --> C
    E -- 예 --> G[선택·비교·보류]
    G --> H{성공 조건 충족}
    H -- 예 --> I[완료·다음 행동]
    H -- 아니요 --> J[조건 수정·재탐색]
    J --> C
    G --> K[중단·취소·복귀]
    K --> C

## 3. 단계별 클라이언트 연결

| 단계 | 원문 기반 내용 | 처리 |
|---|---|---|
| 1 | 같은 제품이 `다시 시작`, `Analog`, `선물` 등 여러 위치에 나타나 중복 제품인지 다른 구성인지 헷갈린다. | 진입·현재 위치 보존 |
| 2 | 카테고리 정의 → 제품 소속 → 다른 분류 → 동일 Product ID 확인 | 핵심 과업 실행 |
| 3 | 분류 기준 설명, 동일 Product 유지, 주·보조 소속 구분, 중복 수량 제거. | 콘텐츠·제품 연결 |
| 4 | 같은 제품과 다른 변형을 구분하고 현재 카테고리 포함 이유를 설명한다. | 완료·보류·재개 |
| 5 | 이름·이미지만 바꿔 중복 제품을 새 후보로 계산한다. | 실패 방지·HOLD |

## 4. 검증
- 원문 행동 흐름을 공통 템플릿으로 대체하지 않고 문서에 직접 반영했다.
- 요구·성공·실패 조건을 각각 서비스 분기와 예외 처리에 연결했다.
- 이 문서는 VC-50 독립 문서이며 통합은 전체 재검토 후 진행한다.

CLIENT=VC-50
STATUS=REWORKED_SOURCE_ALIGNED
REVIEW=FINAL_BATCH_REVIEW_REQUIRED
