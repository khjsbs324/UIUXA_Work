# VC-50 아인-카테고리 경계 이해 — 서비스 흐름도

상태: CANONICAL_REWRITTEN_SOURCE_ALIGNED
작성 방식: 클라이언트별 단일 기준 파일

## 1. 클라이언트 목표와 근거

- 상황: 같은 제품이 '다시 시작', 'Analog', '선물' 등 여러 위치에 나타나 중복 제품인지 다른 구성인지 헷갈린다.
- 목표: 카테고리 기준과 다중 소속 이유를 이해하고 같은 제품을 중복 후보로 세지 않는다.
- 핵심 과업: 카테고리 정의 → 제품 소속 → 다른 분류 경로 → 동일 Product 확인
- 행동 흐름: 카테고리 목록 → 카드 → 다른 분류에서도 보기 → 동일 ID → 원래 위치
- 요구: 분류 기준 설명, 동일 Product 유지, 주·보조 소속 구분, 중복 수량 제거.
- 성공 조건: 같은 제품과 다른 변형을 구분하고 현재 카테고리 포함 이유를 설명한다.
- 실패 조건: 이름·이미지만 바꿔 중복 제품을 새 후보로 계산한다.
- 연결 제품: PROD-030·PROD-060·PROD-100
- 연결 페이지·원문 ID: PAGE-021

## 2. 클라이언트 전용 서비스 흐름

    flowchart TD
    A[VC-50 카테고리 목록] --> B[카테고리 기준·제품 소속]
    B --> C[다른 분류·동일 Product ID 확인]
    C --> D[분류 기준 설명, 동일 Product 유지, 주·보조 소속 구분, 중복 수량 제거.]
    D --> E{근거·지원 상태 확인}
    E -- 부족·미지원 --> F[HOLD·설명·대체 경로]
    F --> C
    E -- 확인 --> G[같은 제품과 다른 변형을 구분하고 현재 카테고리 포함 이유를 설명한다.]
    G --> H{완료 조건 충족}
    H -- 예 --> I[완료·다음 행동]
    H -- 아니요 --> J[조건 수정·재탐색·보류]
    J --> C
    G --> K[중단·취소·복귀]
    K --> C

## 3. 단계별 서비스 상세

| 단계 | 이 클라이언트의 실제 행동 | 페이지·기능 | 상태·분기 |
|---|---|---|---|
| 1 | 같은 제품이 '다시 시작', 'Analog', '선물' 등 여러 위치에 나타나 중복 제품인지 다른 구성인지 헷갈린다. | PAGE-021 진입 | 현재 위치·목적 표시 |
| 2 | 카테고리 기준·주/보조 소속 확인 | 카테고리 설명 | 동일 ID 유지 |
| 3 | 다른 분류·동일 Product 비교 | 교차 탐색 | 중복 수량 제거 |
| 4 | 근거·지원 범위 판단 | 상세·비교·HOLD | 미정 정보 분리 |
| 5 | 같은 제품과 다른 변형을 구분하고 현재 카테고리 포함 이유를 설명한다. | 선택·완료·보류 | 완료·재개 분기 |
| 6 | 이름·이미지만 바꿔 중복 제품을 새 후보로 계산한다. | 오류·취소·복귀 | 실패 방지 |

## 4. 예외·상태 전이

- 결과 없음: 분류 기준 설명, 동일 Product 유지, 주·보조 소속 구분, 중복 수량 제거. 기준으로 재탐색·조건 완화·보류를 제공한다.
- 오류·로딩 실패: 확인된 내용과 현재 위치를 보존하고 실패 영역만 재시도한다.
- 미지원·미정: 사실·가설·HOLD를 분리하고 실행 가능한 기능처럼 표현하지 않는다.
- 중단·복귀: 선택값과 판단 맥락을 보존해 다시 시작할 수 있게 한다.
- 실패 방지: 이름·이미지만 바꿔 중복 제품을 새 후보로 계산한다.

## 5. 연결성 검증

- 요구사항 → 제품 → 페이지 → 흐름 → 다음 화면설계 단계로 추적한다.
- 제품 ID는 연결 후보로만 사용하며 가격·재고·성능을 확정하지 않는다.
- 공통 상태는 재사용하되, 위 흐름의 과업·행동·성공·실패 조건은 VC-50 전용으로 유지한다.

CLIENT=VC-50
PAGES=PAGE-021
PRODUCTS=PROD-030·PROD-060·PROD-100
STATUS=CANONICAL_REWRITTEN_SOURCE_ALIGNED
REQ=REQ-050
REVIEW=READY_FOR_FINAL_REVIEW
## 화면 전달 조건

- 반응형: Desktop·Mobile에서 핵심 과업과 복귀 경로가 유지되어야 한다.
- 접근성: 키보드 포커스, 명확한 Heading/Landmark, 텍스트 상태, 대체 조작을 제공한다.
- 상태: 오류·HOLD·중단·재개 상태를 색상만으로 전달하지 않는다.
- 다음 단계: 이 흐름의 페이지·분기·상태를 화면설계서의 화면 단위로 전개한다.
