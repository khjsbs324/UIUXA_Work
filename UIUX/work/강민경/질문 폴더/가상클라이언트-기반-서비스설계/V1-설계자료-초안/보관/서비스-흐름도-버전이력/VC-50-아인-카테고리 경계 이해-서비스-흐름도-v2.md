# VC-50 아인 — 서비스 흐름도 v2

상태: `REWORKED_CLIENT_SPECIFIC`

## 1. 클라이언트별 목적과 판단

- 상황: 같은 제품이 다시 시작, Analog, 선물 등 여러 위치에 나타나 중복 제품인지 다른 구성인지 헷갈린다.
- 목표: 카테고리 기준과 다중 소속 이유를 이해하고 같은 제품을 중복 후보로 세지 않는다.
- 핵심 과업: 카테고리 정의 → 제품 소속 → 다른 분류 경로 → 동일 Product 확인.
- 행동 흐름: 카테고리 목록 → 카드 → 다른 분류에서도 보기 → 동일 ID → 원래 위치.
- 요구: 분류 기준 설명, 동일 Product 유지, 주·보조 소속 구분, 중복 수량 제거.
- 성공 조건: 같은 제품과 다른 변형을 구분하고 현재 카테고리 포함 이유를 설명한다.
- 실패 조건: 이름·이미지만 바꿔 중복 제품을 새 후보로 계산한다.
- 연결 제품: `PROD-030`, `PROD-060`, `PROD-100`.

## 2. 클라이언트 전용 흐름

    flowchart TD
    A[카테고리 목록] --> B[카테고리 기준 설명]
    B --> C[제품 카드·Product ID 확인]
    C --> D[다른 분류에서도 보기]
    D --> E{동일 제품인가 변형인가}
    E -- 동일 --> F[중복 수량 제거·원래 위치 복귀]
    E -- 변형 --> G[구성·차이 근거 비교]
    F --> H[현재 카테고리 포함 이유 확인]
    G --> H
    H --> I[선택·보류·다른 카테고리 탐색]

## 3. 단계별 상세

| 단계 | 행동 | 화면 역할 | 분기·상태 |
|---|---|---|---|
| 1 | 카테고리 정의 확인 | 카테고리 목록 | 주·보조 소속 기준 표시 |
| 2 | 카드·Product ID 확인 | 제품 카드 | 이름만으로 동일성 확정 금지 |
| 3 | 교차 분류 이동 | 다른 분류에서도 보기 | 동일 ID 유지 |
| 4 | 동일 제품·변형 판단 | 비교 영역 | 구성 차이와 미확정 정보 분리 |
| 5 | 현재 소속 이유 확인 | 카테고리 설명 | 중복 후보 수량 제거 |
| 6 | 선택·보류·복귀 | 선택 상태 | 필터와 판단 맥락 보존 |

## 4. 전용 예외 처리

- Product ID 누락: 이름·이미지만으로 같은 제품이라 확정하지 않고 확인 필요로 표시한다.
- 카테고리 기준 불명확: 기준 설명과 주·보조 소속을 먼저 제공한다.
- 결과 없음: 전체 후보 보기·초기화·보류를 제공한다.
- 실패 방지: 이름·이미지만 바꿔 중복 제품을 새 후보로 계산하지 않는다.

## 5. 추적 검증

- 요구사항: 분류 기준 설명, 동일 Product 유지, 주·보조 소속 구분, 중복 수량 제거.
- 제품: PROD-030·PROD-060·PROD-100.
- V5 행동 흐름과 final-design의 카테고리·상세·교차 이동을 연결했다.

CLIENT=VC-50
STATUS=REWORKED_CLIENT_SPECIFIC
REVIEW=FINAL_BATCH_REVIEW_REQUIRED
INTEGRATION=AFTER_REWORK
