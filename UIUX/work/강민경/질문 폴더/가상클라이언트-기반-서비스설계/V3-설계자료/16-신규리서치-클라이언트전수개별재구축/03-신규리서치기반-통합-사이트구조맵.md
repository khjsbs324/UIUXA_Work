# DAMORI (다모리) 50개 개별 클라이언트 리서치 연동 4-Depth 통합 사이트구조맵 (IA)

문서 ID: `IA-DAMORI-REBUILT-50INDIVIDUAL-FINAL`  
상태: `APPROVED_EXPANDED_50INDIVIDUAL_MAPPED`  
위치: `16-신규리서치-클라이언트전수개별재구축/` 신규 전용 독점 폴더  
근거: `01-신규-시장유저-리서치보고서.md` 및 `02-가상클라이언트-50인-개별상세정의서/` (`VC-01` ~ `VC-50` 50개 개별 독립 파일 100% 연동)

---

## 1. 개요 및 계층 세분화 원칙

50개 개별 가상 클라이언트 정의서(`VC-01` ~ `VC-50`)의 리서치 근거를 바탕으로 4개 계층(1차 GNB → 2차 서브페이지 → 3차 세부 과업 모듈 → 4차 50개 개별 VC 매핑)으로 1:1 완벽 구조화했습니다.

---

## 2. 50개 개별 클라이언트 1:1 매핑 사이트구조맵 (IA 4-Depth)

- **GNB 0: 메인페이지 계층 (`SCR-001 / PAGE-001 홈 메인`)**:
  - `001-PRODUCTS`: 15~50개 전수 라인업 가로 스크롤 카드 카러셀 (`VC-48 태린`, `VC-47 주하`, `VC-42 현서`)
  - `001-SCHEDULE`: 지안(`VC-02`) 24시간 타임라인 시뮬레이터 및 HOLD 공지 배지
  - `001-START`: 하람(`VC-03`) 10초 저부담 사진/1줄/음성/감정 폼 모달
  - `001-TEMPLATE`: 소담(`VC-04`) & 나린(`VC-06`) 원본 데이터 100% 보존 템플릿/자산 미리보기
  - `001-TRUST`: 가람(`VC-11`) & 예린(`VC-27`) 출처 및 확인일이 명시된 신뢰 공지 밴드
  - `001-GUIDE`: 이준(`VC-07`) 중단 후 죄책감 없는 노구 복귀 응원 밴드

- **GNB 1: 기록 방식 찾기 계층 (`PAGE-002`, `PAGE-003`)**:
  - `PAGE-002`: 다온(`VC-08`) 회원가입 없는 4대 자율 필터 칩 탐색 (도윤 `VC-23`, 마루 `VC-14`, 주원 `VC-20`, 시온 `VC-35`, 유나 `VC-41`)
  - `PAGE-003`: 나만의 선택지 기반 성격검사 없는 맞춤 추천 결과 (다온 `VC-08` 무설정 추천)

- **GNB 2: 아날로그 기록 계층 (`PAGE-004` ~ `PAGE-008`)**:
  - `PAGE-004`: 은재(`VC-01`) 180도 평면 펼침 실제본 노트 및 건우(`VC-29`) 슬림 바인더
  - `PAGE-005`: 은재(`VC-01`) 120gsm 미유광 평산지 3종 내지 선택 및 새봄(`VC-15`) 친환경 종이 출처
  - `PAGE-006`: 장문 필기 템포 및 자세 가이드
  - `PAGE-007`: 온유(`VC-10`) 황동 인장 실링 왁스 각인 세트, 민서(`VC-21`), 재희(`VC-25`), 나윤(`VC-36`)
  - `PAGE-008`: 윤슬(`VC-09`) 종이 평량/잉크 번짐률 0% 1대1 수치 비교표 및 채운(`VC-17`)

- **GNB 3: 디지털 기록 계층 (`PAGE-009` ~ `PAGE-012`)**:
  - `PAGE-009`: 지안(`VC-02`), 하람(`VC-03`), 태오(`VC-05`) 디지털 통합 메인
  - `PAGE-010`: 하람(`VC-03`) 10초 빠른 폼, 한결(`VC-16`), 선율(`VC-22`), 유진(`VC-32`)
  - `PAGE-011`: 지안(`VC-02`) 24시간 일간 타임라인, 외부 달력 HOLD 공지 배지, 수현(`VC-28`), 지민(`VC-31`)
  - `PAGE-012`: 태오(`VC-05`) 로컬 IndexedDB 저장 및 PDF/CSV 표준 포맷 내보내기, 해솔(`VC-12`), 서우(`VC-18`)

- **GNB 4: 템플릿과 꾸미기 계층 (`PAGE-013` ~ `PAGE-015`)**:
  - `PAGE-013`: 소담(`VC-04`), 나린(`VC-06`), 아인(`VC-50`) 개념 분리 메인
  - `PAGE-014`: 소담(`VC-04`) 입력 구조(Structure) 템플릿 적용 및 원본보호, 서진(`VC-30`)
  - `PAGE-015`: 나린(`VC-06`) 표현 자산(Asset) 독립 스티커/마스킹 테이프 레이어

- **GNB 5: 기록 가이드 계층 (`PAGE-016` ~ `PAGE-019`)**:
  - `PAGE-016`: 이준(`VC-07`), 여울(`VC-13`) 가이드 메인
  - `PAGE-017`: 다온(`VC-08`), 하윤(`VC-34`) 튜토리얼 없는 작게 시작하기
  - `PAGE-018`: 이준(`VC-07`) 기록 중단 후 미안함 없는 노구 복귀, 지후(`VC-26`)
  - `PAGE-019`: 여울(`VC-13`) WCAG 2.1 스크린리더, 44x44px 터치 타깃, 3px 포커스 링, 초아(`VC-19`), 연우(`VC-33`), 지율(`VC-46`)

- **Footer: 브랜드 & 통합 계층 (`PAGE-020`)**:
  - `PAGE-020`: 아라(`VC-24`), 예린(`VC-27`), 라온(`VC-38`), 은호(`VC-39`), 다빈(`VC-43`), 해나(`VC-45`) 단색 불박 도장 각인 및 "기억의 서재" 서사
